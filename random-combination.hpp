#ifndef _RANDOM_COMBINATION_HPP
#define _RANDOM_COMBINATION_HPP

#include <vector>

#include "vmc-typedefs.hpp"

extern void random_combination (std::vector<int> &v, int r, int n, rng_class &rng);

#endif
